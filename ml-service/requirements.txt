fastapi
uvicorn[standard]
confluent-kafka

# Vision Language Model — Qwen2-VL-2B-Instruct
# CPU-only torch — Docker mein GPU nahi hai, CUDA download waste hai!
# CPU torch = ~200MB vs CUDA torch = ~2.5GB
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

transformers>=4.45.0
accelerate>=0.33.0
# bitsandbytes CPU mode (4-bit GPU ke liye nahi, sirf import ke liye)
bitsandbytes>=0.43.0
qwen-vl-utils

# Image processing
pillow
numpy
matplotlib

# Infrastructure
pydantic
pyyaml
pymongo
python-dotenv
requests
sentinelhub
